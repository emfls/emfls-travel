# EMFLS Travel 디자인 시스템

## Design concept

사진 중심 여행 매거진, 지도 위에 메모한 듯한 편집 그리드, 목적지로 이어지는 큰 타이포그래피.

## Typography

본문은 읽기 쉬운 산세리프, 대형 제목과 편집적 인용은 Georgia 계열 세리프를 사용한다. 제목은 넓은 여백과 짧은 행으로 리듬을 만든다.

## Layout

모바일 우선. 넓은 히어로, 12열로 확장 가능한 카드 그리드, 얇은 구분선, 콘텐츠 사이의 넉넉한 세로 여백을 사용한다.

## Colors

종이색 `#f6f3ed`, 잉크 `#1f2925`, muted green `#e4e8dc`, 주황색 액센트 `#e56b3f`, 보조 sage·blue·rose를 사용한다.

## Navigation

헤더에는 핵심 탐색만 노출한다: 국내, 해외, 가족여행, 아이와, 자동차, 준비. 숙소와 팁은 본문 카드와 연결한다.

## Card system

카드는 eyebrow → 큰 제목 → 짧은 설명 → 이동 링크 순서다. 카드 이미지는 선택 사항이며 로컬 CSS 아트가 없는 사진을 대신한다.

## Photo policy

저작권과 성능을 고려해 외부 이미지 hotlink을 사용하지 않는다. 직접 제작·보유 이미지 또는 라이선스를 확인한 이미지에 한해 출처와 사용 범위를 기록한다. 실제 사진이 추가될 때는 내용을 설명하는 alt, 비율, 대체 상태를 함께 관리하며 SEO 키워드를 alt에 반복하지 않는다. 현재는 CSS visual placeholder를 사용한다.

## Responsive behavior

데스크톱 그리드는 모바일에서 한 열로 내려가고, 헤더 메뉴는 가로 스크롤한다. 버튼과 링크는 터치 가능한 여백을 유지한다.

## Forbidden patterns

SaaS 대시보드형 사이드바, 과도한 배지·통계, 가짜 평점·리뷰, 광고 우선 레이아웃, 이미지가 없으면 무너지는 고정 배경, 대량 자동 생성 SEO 페이지를 금지한다.

Travel 전용 raster 이미지 자산은 프로젝트의 `public/images/`에서 직접 관리한다. 외부 URL, hotlink, 라이선스 불명 이미지는 사용하지 않는다. 현재 생성 이미지는 Hero·목적지 카드·목적지 상세 Hero에서 공유하며, `object-fit: cover`와 명시적 alt로 반응형 crop을 제어한다.
