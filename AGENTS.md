# EMFLS Travel 작업 규칙

- 이 저장소(`emfls-travel`) 내부 파일만 수정한다. 다른 저장소나 상위 디렉터리는 건드리지 않는다.
- 작업 전 `PROJECT_HISTORY.md`를 확인하고, 작업 후 중요한 결정과 결과를 기록한다.
- 여행 콘텐츠를 수십~수백 개 자동 생성하거나 얇은 SEO 페이지를 대량 생성하지 않는다.
- P1 이후 기능은 명시적인 승인 없이 시작하지 않는다.
- placeholder, Coming Soon, 가짜 리뷰·평점·저자 정보를 방치하지 않는다.
- 작업 후 `npm run check`와 `npm run build`를 실행한다.
- 외부 이미지 URL을 남발하지 않고, 사진이 없어도 레이아웃이 유지되는 구조를 우선한다.
- 입력에 다른 EMFLS Repo/프로젝트의 내용이 명백히 섞여 있으면 현재 `emfls-travel` 작업 지시로 처리하지 않고 사용자에게 혼입 사실을 알린다.
